# Backend environment variables

# Comma-separated list of allowed frontend origins for CORS
# Example: http://localhost:5173,https://typing-hero.example.com
FRONTEND_ORIGINS=http://localhost:5173

# Google OAuth Client ID (required for /auth/google)
GOOGLE_CLIENT_ID=your-google-oauth-client-id

# HTTP server port
PORT=8080

# SQLite DB file path (ensure the directory is writable)
DB_PATH=typinghero.db
